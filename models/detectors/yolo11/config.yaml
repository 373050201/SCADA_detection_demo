dataset_config: datasets/SCADA_yolo/data.yaml
pretrained_model: yolo11s.pt
cleanup_models: [yolo11s.pt, yolo26n.pt]
input_size: 960
device: 0
agnostic_nms: false
max_det: 1000
epochs: 100
batch_size: -1
workers: 0
patience: 30
project: runs/scada_yolo11
name: yolo11s_960
output_model: models/weights/best_yolo11s.pt
cls_list: [RD, RS, GD, GS, RDL, RSL, GDL, GSL, Text]
